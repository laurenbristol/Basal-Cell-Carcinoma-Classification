# NBA GAMES 
# There is a file in this that has all of the games from 2016 until 2022 and the statistics of the games 

import pandas as pd 
df = pd.read.csv("nba_games1.csv" , index_col=0)
df 
df = df.sort_values("date") # I need it to be sorted by date 
df = df.reset_index(drop=True) 
df
del df["index"] 
del df ["mp.1"]
del df ["mp_opp.1"]
del df ["index_opp"]
